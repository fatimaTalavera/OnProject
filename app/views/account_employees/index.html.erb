<div class="box box-success">
  <div class="box-header with-border box-success">
    <h3 class="box-title"><%= t('helpers.title.account_employee.list') %></h3>
    <%= link_to "<i class=\"fa fa-plus\"></i>".html_safe, new_account_employee_path, {:class => "btn btn-sm btn-flat bg-green btn-title", :title => "Agregar Certificación"} %>
  </div>
  <div class="box-body">
    <div class="account_employee-search searchList">
      <%= search_form_for(@q, remote: true, :html => { class: 'remote-search' }) do |f| %>
          <div class="input-group">
            <%= f.text_field :certification_contract_name_cont, class: 'form-control', autocomplete: 'off', placeholder: 'Busca por nombre del contrato o por nombre del jornalero' %>
            <div class="input-group-btn">
              <button type="submit" name="button" class="btn btn-default">
                <i class="fa fa-search"></i>
              </button>
            </div>
          </div>
      <% end %>
    </div>
    <div class="overlay-wrapper searchResult table-responsive" id="account_employees-table">
      <%= render 'list', :account_employees => @account_employees %>
    </div>
  </div>
  <div class="box-footer clearfix">
    <div id="paginate-container">
      <ul class="pagination pagination-sm no-margin pull-right">
        <%= paginate @account_employees %>
      </ul>
    </div>
  </div>
</div>