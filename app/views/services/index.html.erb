<div class="box box-success">
  <div class="box-header with-border">
    <h3 class="box-title">Lista de Servicios <%= link_to new_service_path(@service), :class => 'btn btn-success' do %>
          <span class="fa fa-plus"></span>
      <% end %></h3>
  </div>
  <div class="box-body">
    <%= render 'table', services: @services %>
  </div>
</div>
