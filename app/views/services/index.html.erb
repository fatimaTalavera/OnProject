<div class="box box-success">
  <div class="box-header with-border box-success">
    <h3 class="box-title"><%= t('helpers.title.service.list') %></h3>
    <% if (can? :create, Service) %>
        <%= link_to "<i class=\"fa fa-plus\"></i>".html_safe, new_service_path, {:class => "btn btn-sm bg-green btn-title", :title => "Agregar Servicio"} %>
    <% end %>
  </div>
  <div class="box-body">
    <div class="service-search searchList">
      <%= search_form_for(@q, remote: true, :html => { class: 'remote-search' }) do |f| %>
          <div class="input-group">
            <%= f.text_field :name_or_description_cont, class: 'form-control', autocomplete: 'off', placeholder: 'Busca por nombre o descripción' %>
            <div class="input-group-btn">
              <button type="submit" name="button" class="btn btn-default">
                <i class="fa fa-search"></i>
              </button>
            </div>
          </div>
      <% end %>
    </div>
    <div class="overlay-wrapper searchResult table-responsive" id="services-table">
      <%= render 'list', :services => @services %>
    </div>
  </div>
  <div class="box-footer clearfix">
    <div id="paginate-container">
      <ul class="pagination pagination-sm no-margin pull-right">
        <%= paginate @services %>
      </ul>
    </div>
  </div>
</div>