<div class="box box-success">
  <div class="box-header with-border">
    <h3 class="box-title">Lista de Clientes <%= link_to new_client_path(@client), :class => 'btn btn-success' do %>
        <span class="fa fa-plus"></span>
    <% end %></h3>
  </div>
    <div class="box-body">
      <%= render 'table', clients: @clients %>
    </div>
</div>
