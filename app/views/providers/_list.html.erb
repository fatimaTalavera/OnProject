<table class="table table-bordered table-condensed">
  <thead>
  <tr>
    <th>Nombre o Razón Social</th>
    <th>RUC</th>
    <th>Teléfono</th>
    <th>Correo</th>
    <th>Dirección</th>
    <th></th>
  </tr>
  </thead>

  <tbody>
  <% providers.each do |provider| %>
      <tr>
        <td><%= disminuir(provider.name) %></td>
        <td><%= provider.ruc %></td>
        <td><%= provider.phone %></td>
        <td><%= disminuir(provider.email) %></td>
        <td><%= disminuir(provider.address) %></td>
        <td>
          <% if (can? :update, Provider) %>
              <%= link_to "<i class=\"fa fa-pencil\"></i>".html_safe, edit_provider_path(provider), {:class => "btn btn-xs bg-orange btn-flat"} %>
          <% end %>
        </td>
      </tr>
  <% end %>
  </tbody>
</table>

