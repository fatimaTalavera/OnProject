<div class="box box-success">
  <div class="box-header with-border">
    <h3 class="box-title"><%= t('helpers.title.contract.new') %></h3>
  </div>
  <%= render 'form', :contract => @contract %>
</div>
