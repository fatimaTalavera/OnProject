<div class="box box-success">
  <div class="box-header with-border">
    <h3 class="box-title"><%= "Rubro" %></h3>
  </div>
  <div class="box-body">
    <div class="col-md-12 form-group">
      <div class="col-sm-3">
        <strong>Nombre:</strong>
        <%= @rubro.name %>
      </div>
      <div class="col-sm-3">
        <strong>Unidad de medida:</strong>
        <%= @rubro.measurement_unit %>
      </div>
      <div class="col-sm-3">
        <strong>Utilidad:</strong>
        <%= @rubro.utility %> %
      </div>
      <div class="col-sm-3">
        <strong>Precio:</strong>
        <%= number_to_currency_py(@rubro.final_price.to_i) %>
      </div>
    </div>

    <div class="col-md-12 form-group">
      <div class="col-md-8">
        <table class="table table-bordered table-condensed", cellspacing="0" width="100%">
          <thead>
          <tr>
            <th>Material</th>
            <th>Precio Unitario</th>
            <th>Cantidad</th>
            <th>Subtotal</th>
          </tr>
          </thead>
          <tbody>
          <% @rubro.rubro_material_details.each do |detalle| %>
          <tr>
              <td><%= detalle.material.name %></td>
              <td><%= number_to_currency_py(detalle.material.price.to_i) %></td>
              <td><%= detalle.quantity %></td>
              <td><%= number_to_currency_py(detalle.subtotal.to_i) %></td>
          </tr>
          <% end %>
          </tbody>
          <thead>
          <tr>
            <th>Servicio</th>
            <th>Precio Unitario</th>
            <th>Cantidad</th>
            <th>Subtotal</th>
          </tr>
          </thead>
          <tbody>
          <% @rubro.rubro_service_details.each do |detalle| %>
          <tr>
              <td><%= detalle.service.name %></td>
              <td><%= number_to_currency_py(detalle.service.price.to_i) %></td>
              <td><%= detalle.quantity %></td>
              <td><%= number_to_currency_py(detalle.subtotal.to_i) %></td>
          </tr>
          <% end %>
          </tbody>
        </table>
      </div>
    </div>

  </div>
  <div class="box-footer">
    <div class="pull-right">
      <% if (can? :update, Rubro) %>
          <%= link_to t('button.edit'), edit_rubro_path(@rubro), {:class => "btn btn-flat bg-blue"} %>
      <% end %>
      <%= link_to t('button.back'), url_for(:back), {:class => "btn btn-flat btn-default"} %>
    </div>
  </div>
</div>