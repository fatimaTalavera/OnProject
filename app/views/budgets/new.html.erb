<div class="box box-success">
  <div class="box-header with-border">
    <h3 class="box-title"><%= t('helpers.title.budget.new') %></h3>
  </div>
      <%= render 'form', :budget => @budget %>
  </div>
