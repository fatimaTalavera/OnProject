<div class="box box-success">
  <div class="box-header with-border box-success">
    <h3 class="box-title"><%= t('helpers.title.budget.list') %></h3>
    <% if (can? :create, Budget) %>
        <%= link_to "<i class=\"fa fa-plus\"></i>".html_safe, new_budget_path, {:class => "btn btn-sm btn-flat bg-green btn-title", :title => "Agregar Presupuesto"} %>
    <% end %>
  </div>
  <div class="box-body">
    <div class="budget-search searchList">
      <%= search_form_for(@q, remote: true, :html => { class: 'remote-search' }) do |f| %>
          <div class="input-group">
            <%= f.text_field :client_name_or_state_cont, class: 'form-control', autocomplete: 'off', placeholder: 'Busca por nombre o estado' %>
            <div class="input-group-btn">
              <button type="submit" name="button" class="btn btn-default">
                <i class="fa fa-search"></i>
              </button>
            </div>
          </div>
      <% end %>
    </div>
    <div class="overlay-wrapper searchResult table-responsive" id="budgets-table">
      <%= render 'list', :budgets => @budgets %>
    </div>
  </div>
  <div class="box-footer clearfix">
    <div id="paginate-container">
      <ul class="pagination pagination-sm no-margin pull-right">
        <%= paginate @budgets %>
      </ul>
    </div>
  </div>
</div>