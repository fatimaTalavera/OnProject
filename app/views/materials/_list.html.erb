<table class="table table-bordered table-condensed table-list">
  <thead>
  <tr>
    <th>Nombre</th>
    <th>Descripción</th>
    <th>Cantidad</th>
    <th>Unidad de medida</th>
    <th>Precio Unitario</th>
    <th class="table-actions"></th>
  </tr>
  </tr>
  </thead>

  <tbody>
  <% materials.each do |material| %>
      <tr>
        <td><%= material.name %></td>
        <td><%= material.description %></td>
        <td><%= material.quantity %></td>
        <td><%= material.measurement_unit %></td>
        <td><%= number_to_currency_py(material.price) %></td>

        <td>
          <% if (can? :update, Material) %>
              <%= link_to "<i class=\"fa fa-pencil\"></i>".html_safe, edit_material_path(material), {:class => "btn btn-xs bg-orange", :title => "Editar"} %>
          <% end %>
        </tr>
  <% end %>
  </tbody>
</table>