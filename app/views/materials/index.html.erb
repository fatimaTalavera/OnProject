<div class="box box-success">
  <div class="box-header with-border box-success">
    <h3 class="box-title"><%= t('helpers.title.material.list') %></h3>
    <% if (can? :create, Material) %>
        <%= link_to "<i class=\"fa fa-plus\"></i>".html_safe, new_material_path, {:class => "btn btn-sm btn-flat bg-green btn-title", :title => "Agregar Material"} %>
    <% end %>
  </div>
  <div class="box-body">
    <div class="material-search searchList">
      <%= search_form_for(@q, remote: true, :html => { class: 'remote-search' }) do |f| %>
          <div class="input-group">
            <%= f.text_field :name_or_description_cont, class: 'form-control', autocomplete: 'off', placeholder: 'Busca por nombre o descripción' %>
            <div class="input-group-btn">
              <button type="submit" name="button" class="btn btn-default">
                <i class="fa fa-search"></i>
              </button>
            </div>
          </div>
      <% end %>
    </div>
    <div class="overlay-wrapper searchResult table-responsive" id="materials-table">
      <%= render 'list', :materials => @materials %>
    </div>
  </div>
  <div class="box-footer clearfix">
    <div id="paginate-container">
      <ul class="pagination pagination-sm no-margin pull-right">
        <%= paginate @materials %>
      </ul>
    </div>
  </div>
</div>
