 <div class="box">
   <div class="box-header with-header">
     <div class="box-title">Lista de Materiales <%= link_to new_material_path(@material), :class => 'btn btn-success' do %>
           <span class="fa fa-plus"></span>
       <% end %></div>
     <br>
     <br>
     <%= render 'table', material: @material %>
     <br>
   </div>
 </div>